To create an exe:
  1. pip install -r requirements.txt
  2. pip install pyinstaller
  3. pyinstaller -y --onefile [SCRIPT] --copy-metadata pyppeteer
    *copy metadata gathers version info to avoid exception
